# 项目工作约束

1. 工作前先读取 `README.md`、`docs/content-policy.md` 和相关脚本说明，并运行 `git status --short`。
2. 工作树出现不在既知范围内的改动时，先报告，不覆盖、删除、改名或暂存它们。
3. `ops-local/` 永不提交，不能在公开内容中引用其中的文件。
4. 日常群消息只显示不超过 5 条 `core`；网页可补充 `extended`，但日常总条目不超过 10 条，并执行 14 天去重。
5. 所有课程群共用一份 `YYYY-MM-DD.txt`；标题、引文、DOI、URL 与当天详版 URL 的事实一致。会议信息增刊同样只生成一份文本。
6. 课程入口只放在网页，不能出现在群消息。标题不得使用小白菊或字符边框。
7. 所有远程写操作、Git 暂存、提交、推送、部署和定时任务都须获得用户在当前任务中的明确授权；不得擅自创建或改写定时任务。
8. 公开身份以 `config/site.json` 为唯一来源：名称为「连享会 · 快讯」，公开首页为 `https://lianxhcn.github.io/brief/`，日期页采用 `/YYYYMMDD/`。本地目录名不得出现在公开内容。
