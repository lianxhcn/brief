# 自动化与发布长期规则

canonical owner：运行、审核、发布、失败及日志。

## 1. 状态与授权

- A-01 [HARD]：当前稳定链条为 `approved push main → GitHub Actions → build → deploy Pages`。完整每日采集、核验、审批、提交和微信群发送尚未全部完成。已有 workflow_dispatch 能力不等于授权。
- A-02 [HARD]：D-20260908-06 授权恢复每日快讯生产与自动发布，替代此前日常发布冻结。授权限本项目的既定内容和网站流程；不扩展到其他项目或微信发送。历史冻结记录只作追踪。
- A-03 [HARD]：日常流程由代理按 canonical 内容规则核验并执行发布检查，通过后可提交、推送和发布，无需逐日再次征求批准。证据不足、未检索、校验失败或重大未决问题时保留草稿并报告，不绕过质量闸门；不自动操作微信。
- A-04 [HARD]：暂存、提交、推送、部署、远程写入和定时任务需要当前任务明确授权，不沿用 Task 06 / 08 历史权限。ALL-01 不执行 Git 写入或触发工作流。
- A-05 [HARD]：每日流程授权不豁免质量检查；人工新暂停指令优先，遇到真实阻断停止发布并记录。

## 2. 隔离与失败

- A-06 [HARD]：ops-local 永不提交；候选、去重、人工备注、共享目录和分发个人信息不得公开。不把 _site、.quarto、缓存或运行日志当发布源码。
- A-07 [HARD]：测试 / DEMO 不进入生产索引或正式输出。后续任务可在明确隔离的测试目录使用 fixtures，不标为新正式期次；ALL-01 只做静态文档验证。
- A-08 [HARD]：无法核验、质量不足或审核未完成时记录缺口 / no-release，不制造内容或改成 published；充分检索后仅数量不足 10 条按 C-09 保留草稿、条数和原因，可进入发布审核；错误不自动转成“已发布”。日志记录阶段、日期、原因和下一步，不含凭据。
- A-09 [DEFAULT]：Task 11 可采用阶段日志、有限重试、幂等检查和可恢复审核记录；具体机制需实施验证，不能声称已完成。
- A-10 [HARD]：每日 (含周末) 北京时间 Asia/Shanghai 07:15 触发检索、核验、生成和发布流程。07:15 为开始时间，网页在检查和 GitHub Pages 部署完成后上线；不承诺精确分钟上线。使用本任务的 Codex 本地定时任务，运行条件以官方说明及实际工具状态为准。
- A-11 [HARD]：每轮执行 canonical 一致性检查和当期内容审核；定时创建成功不等于完整链路已实测。首次成功部署后记录端到端证据，保留未解决的覆盖与采集债务。

## 3. 恢复记录

[DEFAULT] 后续验收记录至少包含任务、规则 ID、测试 / 人工复核证据、剩余债务、日期和结论。当前恢复依据为 D-20260908-06；先前临时授权和冻结记录不再指导日常执行。

## 4. 当前单次例外记录

2026-09-06，用户在 R03 后明确授权当前网站推送并发布一次，用于手机人工验收。按 A-02/A-04 执行，范围与有效期见 [D-20260906-05](decision-log.md)。此记录不解除新正式期次暂停，不表示 Task 09/10/11 已全部验收。

## 当前修订范围

D-20260908-02 为历史本地修订范围；当前 D-20260908-06 允许已审阅修订随首轮合格日常内容一并发布。D-20260908-01 为已用尽的历史发布记录，不作为当前执行规则。

A-12 [HARD]：本地及 CI 发布验收共同检查分类检索记录、网页联合数量或少量内容记录、arXiv PDF 构造、引文一致性、会议有效性、标签及公开历史索引。生成/构建成功不能替代编辑验收。未到达发布条件的材料保留草稿和真实错误，不伪造 0 候选或已发布状态。

A-13 [HARD]：每日生成并校验正式微信 TXT 后，执行 `python scripts/copy_wechat.py`，将 2026-09-08 起的正式文件另存到用户指定共享目录。目的地配置保存在不提交的 ops-local/wechat-delivery.json；字节一致性校验、同内容幂等，不复制 DEMO 或早于起始日的文件。目标内容不同时不静默覆盖，报告冲突。共享复制失败单独报告，不声称已交付，也不自动发送微信。

A-14 [HARD]：每日运行及完成报告须明确目标期次日期。核验该日期的 JSON、网页与 TXT、包含该期次的远程提交及其 Pages 部署结果；共享交付另核对同日期文件的字节一致性。旧期次部署成功不能作为当期完成证据。中途收到追问时保留当期任务，不能转回上一轮修订结果。
