# 首页与分页本地验收 (2026-09-08)

本轮五项预览反馈已在本地实现，待用户查看。未提交、推送或发布；不改变原有发布冻结状态。

## 实现结果

- 日常正文“本期重点”“延伸阅读”均为墨绿色 `#1e5d46`，修复原样式仍引用“延伸信息”的问题。
- 四个栏目及往期采用每页最多 10 条的独立静态分页，第一页地址保持不变，后续页为同目录 `名称-2.html`。每页只包含本页条目，提供上下两处分页导航。未满一页不显示无效翻页按钮。
- 首页展示最近 3 个正式发布日期；当前是 9 月 8、5、4 日。“最新期次”标题上方实际 margin 从 17px 增至 25.5px。栏目框只保留名称，手机显示两列，320px 宽度下名称不折行。
- 桌面课程区保留既有海报；手机端显示同一审核配置生成的课程文字卡片，包含标题、三次完整日期和详情链接。未增加图片、课程事实或外部上传。
- 移除单页筛选框，关键词查询使用全站搜索。检索 Hooks 能从新推文第一页找到第三页及对应历史内容。

## 验收证据

| 对象 | 当前结果 |
|---|---|
| 新推文 | 29 条，3 页，分别为 10 / 10 / 9 |
| 新论文 | 18 条，2 页，分别为 10 / 8 |
| 新方法 | 4 条，1 页 |
| 会议征稿 | 4 条，1 页 |
| 往期 | 3 期，1 页；21 期跨月份测试验证 10 / 10 / 1 |
| 测试 | 124 项单元测试通过，包括 5 项新增分页测试 |
| 构建 | Quarto 完整渲染 16 页成功；自动索引、链接和搜索验收通过 |
| 浏览器 | 实际点击新推文第二页、末页与论文第二页；搜索找到跨页文章 |
| 响应式 | 320px、390px、1440px 宽度检查；无横向溢出；手机日期约 17.85px，桌面海报保留 |

新增分页测试覆盖 0、1、10、11、20、21、101 条边界、顺序与完整性、四类独立分页、首末页链接及过期生成文件所有权。构建后验收检查预期分页存在、单页数量和内部日志不进入搜索。

构建时发现宽泛的 archive 通配符会匹配日志基线页，已改为数字分页通配符并排除 docs/logs；最终构建无此类页面，`_site/logs` 不存在。浏览器实测发现 Quarto 默认间距覆盖自定义 CSS，已修正优先级并重新测量。相关问题均已解决。

执行命令包括 `python -m unittest discover -s scripts -p 'test_*.py'`、`quarto render`、`python scripts/build_website.py --check`、`python scripts/validate_website.py`、`python scripts/validate_navigation.py` 和 `git diff --check`。最终 CSS 小幅修正已同步 `_site/styles.css` 并经浏览器复核。

## 文件与规则

核心改动：`scripts/build_catalog_pages.py`、`scripts/build_website.py`、`scripts/website_promotions.py`、`scripts/validate_website.py`、`styles.css`、`_quarto.yml`；同步既有网站测试并新增 `scripts/test_catalog_pagination.py`。重新生成首页、往期、栏目页面，新增新推文第 2/3 页及新论文第 2 页。

规则仍归现有 `ui-rules.md` 的 U-12/U-13、`architecture.md` 和 `decision-log.md` 的 D-20260908-03 管理。本轮无内容筛选或自动发布政策变化。

本轮读取项目 AGENTS、README、canonical rules、任务与实现债务记录，以及相关生成器、校验器、样式、配置、课程 SVG 和测试。未重新联网核验课程来源；课程日期沿用现有审核配置。未改变此前期次修订与历史补录成果，也不宣称 Task 11 全部完成。

预览：首页 http://127.0.0.1:8765/；分页示例 http://127.0.0.1:8765/topics/lianxh-new-2.html 。用户人工验收 pending。
