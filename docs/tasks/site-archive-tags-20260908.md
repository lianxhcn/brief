# 统一期次名称、年度归档与标签链接 (2026-09-08)

三项预览反馈已本地实现，待用户验收；未提交、推送或发布。

- 首页、往期和正式期次正文共用 `site_config.issue_title`，格式为 `连享会 · 快讯 | YYYY.MM.DD`。9 月 4 日旧标题不再直接用于首页入口。原始 JSON 和发布证据保持，避免为 UI 改写历史记录。
- 往期按年份、月份两级原生 details/summary 折叠，年份默认展开、月份默认收起，月份内完整列出每日链接。每页一个完整年份，跨年提供年份导航；不再按 10 期频繁分页。仅一页时没有页码，年/月摘要分别显示期次数量。
- 标签点击使用 Quarto 全站关键词搜索，编码 q 和 show-results 参数。覆盖日常、历史、栏目卡片和标签概览；软件、方法、领域元数据也可点击。此为相关内容搜索，不是严格标签等值筛选。

## 验收

127 项测试通过，包含 3 年、36 个月的跨年完整性检查，月份不跨页，保留四栏目每页 10 条的原有测试；另测试旧 title 不渗入新展示、中文及特殊字符标签编码。Quarto 16 页渲染成功，生成一致性、公开搜索、内部链接、导航校验通过。构建后 validator 新增首页和往期入口命名检查，以及往期年/月数量及旧提示排除检查。

实际浏览器点击 9 月折叠并进入 9 月 4 日页面，标题正确；点击“敏感性分析”标签后，搜索框自动填入该词，结果含正式期次、栏目与历史内容。390px 手机截图复核日期链接与年/月层级清楚，无横向溢出。

完整测试首次与 Quarto 渲染重叠，两个搜索产物测试因构建正在重建 _site 而暂时缺文件；构建结束后重新执行，127 项全部通过。代码没有为此放宽检查。CSS 末尾空行已清理。

## 变更和规则

核心文件：`scripts/site_config.py`、`scripts/website_tags.py`、`scripts/render_issue_pages.py`、`scripts/build_catalog_pages.py`、`scripts/validate_website.py`、相关回归测试、`styles.css` 和生成页面。

长期规则更新现有 `ui-rules.md` 的 U-12、U-14/U-15/U-16，架构更新统一标题及标签搜索模块，裁定记录 D-20260908-04。前一轮往期每页 10 条的实现记录是历史快照，现由本轮年度折叠方案替代；四栏目分页规则不变。

预览：http://127.0.0.1:8765/archive.html 。无需安装软件或建立新的搜索后端。未检索或增加内容来源，原有内容修订、自动化未决事项和发布冻结保持。
